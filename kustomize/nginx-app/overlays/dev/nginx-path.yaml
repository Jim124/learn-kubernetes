apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:
  template:
    spec:
      containers:
        - name: nginx
          image: nginx:1.27.1
          resources:
            limits:
              cpu: '500m'
              memory: '256Mi'
            requests:
              cpu: '500m'
              memory: '256Mi'
